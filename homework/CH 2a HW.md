# T-SQL Chapter 2a HW

1. FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY
1. Specifies the tables you want to query and table operators that operate those tables.
1. Specifies a predicate or logical expression to filter the rows returned by the FROM phase.
1. Arranges the rows returned by the previous logical query processing phase in groups.
1. Evaluates the groups and those having a TRUE are returned to the next logical query phase.
1. Specifies the attributes you want to return in the results table.
1. Removes duplicates from the queried results.
1. Sorts the rows in the output for presentation purposes.
1. Sets an upper limit on the number of tuples returned.
1. Limits the number or percentage of rows your query returns.
1. OFFSET indicates the number of rows to skip  and FETCH indicates the number of rows to filter after the skipped rows.